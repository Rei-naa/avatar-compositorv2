node_modules/

# Rendered outputs (keep the folder, ignore the media)
outputs/*
!outputs/.gitkeep

# Sample/input media is large — not committed. Drop files in assets/ or pass paths.
assets/

*.log
.DS_Store
